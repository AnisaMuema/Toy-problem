// Creating a variable with an annonymous function, that allows input of speed value
let carSpeed = function(speed){
    let driverSpeed = 70;
    let addDemerits = '';
    if(driverSpped < 70){
      return 'Ok';
      addDemerits = 0;
    }
    // this else statement is for adding a demerit point for every 5km/s, when the speed is above 70
    else{
      addDemerita =+ 1 && driverSpeed > 70*(70/5);
    }
  }
  